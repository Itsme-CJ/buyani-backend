# --- JPA & Database Configuration ---
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.datasource.url=jdbc:mysql://localhost:3306/buyani?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.liquibase.change-log=classpath:/db/db.changelog-root.yaml

# --- REST & Server ---
spring.data.rest.base-path=api
spring.messages.basename=lang/messages
server.port=9000
server.forward-headers-strategy=native

# fix controller conflict with Spring Data REST
spring.data.rest.detection-strategy=annotated

# --- CORS (mobile app / frontend) ---
cors.allowedOrigins=http://192.168.0.104:3000,http://localhost:3000

# --- Twilio (SMS) ---
twilio.accountSId=YOUR_TWILIO_ACCOUNT_ID
twilio.authId=YOUR_TWILIO_AUTH_ID

# --- JWT Token Expiration (optional) ---
JWT_EXPIRATION_MS=3600000
JWT_REFRESH_EXP_MS=86400000

# --- Mail Server (optional, for email notifications) ---
# --- Mail Server ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sijeyqt@gmail.com
spring.mail.password=jkjnguvzoovnkaft
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# These match the @Value annotations in EmailService
mail.server.name=BuyAni App
mail.server.from=sijeyqt@gmail.com
base.url=http://192.168.0.104:3000

# --- Storage (Backblaze B2) ---
STORAGE_URL=s3.us-west-004.backblazeb2.com
STORAGE_PATH_URL=https://f004.backblazeb2.com/file/alchives-cdn
STORAGE_ACCESS_KEY=
004f42af37dc7220000000001
STORAGE_SECRET_KEY=y
K004l/Qaao9hUqTjDm+FHACziTni5FQ

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
